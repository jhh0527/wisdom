# 로컬 Chatterbox (--backend chatterbox --chatterbox-mode local) 전용.
#
# --- Python 버전 ---
# 3.14 등 최신 버전은 의존 패키지(spacy-pkuseg 등)에 미리 빌드된 wheel이 없어
# 소스 컴파일이 걸리고, 그때 Visual C++ Build Tools 가 없으면 설치가 실패합니다.
# 권장: Python 3.12 또는 3.11 (64비트). setup_chatterbox_venv.ps1 로 3.12 venv 생성 가능.
#
# --- spacy-pkuseg / MSVC 오류가 날 때 ---
# A) https://visualstudio.microsoft.com/visual-cpp-build-tools/
#    "Desktop development with C++" 또는 "C++ 빌드 도구" 설치 후 다시 pip install
# B) 또는 Python 3.12 가상환경에서 다시 설치 (wheel 사용 가능성↑)
#
# --- torch ---
# https://pytorch.org 에서 OS·CUDA에 맞는 pip 한 줄을 먼저 실행한 뒤 아래 줄을 설치하세요.
chatterbox-tts>=0.1.0
# GUI 「Chatterbox (HTTP)」용 로컬 브리지: python run_chatterbox_http_server.py 또는 run_chatterbox_server.bat
# CHATTERBOX_HF_INSECURE_SSL: HF·GitHub(requests) 등 HTTPS 기본 검증 완화(미설정=1). 끄려면: =0
